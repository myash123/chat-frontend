import ChatModeNav from '../ChatModeNav/ChatModeNav';

const ChatPanel = () => {
  return (
    <div className="flex p-2 border border-red-600">
      <ChatModeNav />
    </div>
  )
}

export default ChatPanel;